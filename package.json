{
  "name": "crownie-cross-chain-bridges",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "dev": "pnpm --filter main-interface dev",
    "dev:icp": "pnpm --filter fusion-icp-interface dev",
    "dev:etherlink": "pnpm --filter fusion-etherlink-interface dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
