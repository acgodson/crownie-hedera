{
  "name": "crownie-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "dev": "pnpm --filter main-interface dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "deploy:icp": "pnpm --filter @crownie-bridge/fusion-icp-bridge deploy:canister",
    "deploy:eth": "pnpm --filter @crownie-bridge/fusion-icp-bridge deploy:contracts"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
